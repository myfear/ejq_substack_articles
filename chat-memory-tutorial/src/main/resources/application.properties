#Ollama Dev Service
quarkus.langchain4j.ollama.chat-model.model-name=llama3.1
quarkus.langchain4j.ollama.log-requests=true
quarkus.langchain4j.ollama.log-responses=true


# Use custom JPA-based memory store (MonitoredChatMemoryStore wrapping JPAChatMemoryStore)
# The custom store is configured via CDI beans, not through this property